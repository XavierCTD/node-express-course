Question 1. In this lesson, you created a middleware function called 
            asyncWrapper. Why?


Answer: To be clear, 'asyncWrapper' is NOT a method. It is a customed function that can be used. 
        The reason why I created this middleware function is because I can be able to wrap the try and catch method from controller foldler, 
        simplfy/seperate the async and await promises so that the code is less tedious and combine all CRUD methods into a single function.


Question 2. Suppose that you want to make sure that both a status code and 
            an error message are sent back to the 
            user when they request the URL for a task that does not exist. 
            Assume that you’ve created a CustomAPIError class and an error 
            handler that references that class. 
            Complete the code:


Answer:            

const getTask = asyncWrapper(async (req, res, next) => {  
  const { id: taskID } = req.params;  
  const task = await Task.findOne({ _id: taskID });  
  if (!task) {  
    // your code here
    return res.status(404).json({`Error: No task with id: ${taskID}`});
  }  
  res.status(200).json({ task });  
});  